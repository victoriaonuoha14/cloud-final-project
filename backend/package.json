{
  "name": "todo-backend",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp src/* dist/",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}